<div class="container">
  {{#each albumsArray}}
    <div class="card">
      <picture>
        <img src="{{this.images.0.url}}" alt="{{this.name}}" />
      </picture>
      <p>{{this.name}}</p>
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </div>
  {{/each}}
</div>